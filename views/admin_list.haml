#header
  %h1
    Articles
  %ul.nav.nav-pills
    %li
      %a.btn.btn-primary{:href => "/admin/articles/new"} Create article

.row.articles.col-lg-12
  -for a in @articles do
    %article.well.article{:id => "article#{a.id}"}
      %h3= a.title
      .date
        = a.created_at.strftime("%d %b %Y %H:%M")
      %p
        Comments:
        = a.comments.count
      .actions
        %a.btn.btn-default{:href => "/admin/articles/#{a.id}"} Show
        %a.btn.btn-default{:href => "/admin/articles/#{a.id}/edit"} Edit
        %form.publish.inline{action: "/admin/articles/#{a.id}/publish", method: :post, "ispublic" => a.ispublic}
          %input.btn.btn-default{:value => (a.visible) ? "Hide" : "Publish", :type => "submit" }
        %form.remove.inline(action="/admin/articles/#{a.id}/destroy" method="post")
          %input.btn.btn-default{:value => "Delete", :type => "submit" }